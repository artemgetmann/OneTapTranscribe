OPENAI_API_KEY=

# Optional but recommended when backend is publicly reachable.
APP_CLIENT_TOKEN=

# Optional (comma-separated). Needed only for browser/web clients.
# Native iOS app requests do not require CORS.
CORS_ALLOW_ORIGINS=
